[project]
name = "ntds-organiser"
version = "0.1.0"
description = "NTDS database organiser"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
ntds-organiser = "ntds_organiser:main"
